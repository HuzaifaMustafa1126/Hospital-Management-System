import { loginSchema } from '../validators/auth.validator.js'; import { authService } from '../services/auth.service.js'; import { auditService } from '../services/audit.service.js';
export const authController = { login: async (req, res) => { const { email, password } = loginSchema.parse(req.body); const result = await authService.login(email, password); await auditService.record({ userId: result.user.id, action: 'LOGIN', entity: 'AUTH', entityId: result.user.id, ipAddress: req.ip, userAgent: req.get('user-agent') }); res.json({ success: true, message: 'Login successful', data: result }); }, me: async (req, res) => res.json({ success: true, message: 'Authenticated user retrieved', data: req.user }), logout: async (req, res) => { await auditService.record({ userId: req.user.id, action: 'LOGOUT', entity: 'AUTH', entityId: req.user.id, ipAddress: req.ip, userAgent: req.get('user-agent') }); res.json({ success: true, message: 'Logout recorded' }); } };
