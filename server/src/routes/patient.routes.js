import { Router } from 'express'; import { patientController } from '../controllers/patient.controller.js'; import { authenticate, requirePermission } from '../middleware/auth.middleware.js'; import { asyncHandler } from '../utils/async-handler.js';
export const patientRouter = Router(); patientRouter.use(authenticate); patientRouter.post('/', requirePermission('PATIENT_CREATE'), asyncHandler(patientController.create)); patientRouter.get('/search', requirePermission('PATIENT_SEARCH'), asyncHandler(patientController.search)); patientRouter.get('/', requirePermission('PATIENT_VIEW'), asyncHandler(patientController.list)); patientRouter.get('/:id', requirePermission('PATIENT_VIEW'), asyncHandler(patientController.get)); patientRouter.put('/:id', requirePermission('PATIENT_UPDATE'), asyncHandler(patientController.update)); patientRouter.delete('/:id', requirePermission('PATIENT_DELETE'), asyncHandler(patientController.remove));
