import { Router } from 'express'; import { userController } from '../controllers/user.controller.js'; import { authenticate, requireRole } from '../middleware/auth.middleware.js'; import { asyncHandler } from '../utils/async-handler.js';
export const userRouter = Router(); userRouter.use(authenticate, requireRole('SUPER_ADMIN')); userRouter.get('/', asyncHandler(userController.list)); userRouter.get('/:id', asyncHandler(userController.get)); userRouter.post('/', asyncHandler(userController.create)); userRouter.put('/:id', asyncHandler(userController.update)); userRouter.patch('/:id/status', asyncHandler(userController.status));
