import { useAuth } from '../context/AuthContext'; export function DashboardPage() { const { user } = useAuth(); return <><h2 className="text-2xl font-bold">Hospital Management System</h2><p className="mt-1">Welcome back, {user?.firstName}.</p><div className="mt-6 rounded bg-white p-5 shadow"><p className="text-sm text-slate-500">Signed in as</p><p className="font-semibold">{user?.roles.join(', ')}</p></div></>; }
