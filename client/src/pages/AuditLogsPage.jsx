import { useEffect, useState } from 'react'; import { api } from '../services/api'; export function AuditLogsPage() { const [logs, setLogs] = useState([]); useEffect(() => { api.get('/audit-logs').then((r) => setLogs(r.data.data)); }, []); return <><h2 className="mb-5 text-2xl font-bold">Audit Logs</h2><div className="overflow-x-auto rounded bg-white shadow"><table className="w-full text-left text-sm"><thead className="bg-slate-100"><tr><th className="p-3">Date</th><th>User</th><th>Action</th><th>Entity</th><th>Entity ID</th></tr></thead><tbody>{logs.map((log) => <tr className="border-t" key={log.id}><td className="p-3">{new Date(log.createdAt).toLocaleString()}</td><td>{log.user ? `${log.user.firstName} ${log.user.lastName}` : 'System'}</td><td>{log.action}</td><td>{log.entity}</td><td>{log.entityId || '—'}</td></tr>)}</tbody></table></div></>; }
