import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { patientService } from '../services/patient.service'; import { useAuth } from '../context/AuthContext';
export function PatientListPage() { const { user } = useAuth(); const admin = user?.roles.includes('SUPER_ADMIN'); const [data, setData] = useState({ items: [] }); const [search, setSearch] = useState(''); const [error, setError] = useState(''); const load = async (term = '') => { try { setData((await patientService.list({ page: 1, limit: 20, search: term || undefined })).data.data); } catch (e) { setError(e.response?.data?.message || 'Unable to load patients'); } }; useEffect(() => { void load(); }, []); return <><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><h2 className="text-2xl font-bold">Patients</h2><div className="flex gap-2">{!admin && <Link className="rounded bg-sky-700 px-4 py-2 text-white" to="/reception/patients/register">Register Patient</Link>}<Link className="rounded border px-4 py-2" to={admin ? '/admin/patients' : '/reception/patients/search'}>Search</Link></div></div><form className="mb-4 flex gap-2" onSubmit={(e) => { e.preventDefault(); void load(search); }}><input className="w-full max-w-lg rounded border p-2" value={search} onChange={(e) => setSearch(e.target.value)} placeholder="CNIC, patient number, phone, or name"/><button className="rounded bg-slate-800 px-4 text-white">Search</button></form>{error && <p className="mb-3 text-red-700">{error}</p>}<div className="overflow-x-auto rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-3">Patient No.</th><th>Name</th><th>CNIC</th><th>Phone</th><th>Doctor</th><th>Status</th><th className="p-3">Action</th></tr></thead><tbody>{data.items.map((p) => <tr className="border-t" key={p.id}><td className="p-3 font-medium">{p.patientNumber}</td><td>{p.firstName} {p.lastName}</td><td>{p.cnic}</td><td>{p.phone}</td><td>Dr. {p.doctor.firstName} {p.doctor.lastName}</td><td>{p.isActive ? 'Active' : 'Inactive'}</td><td className="p-3"><Link className="text-sky-700" to={`/patients/${p.id}`}>View</Link>{admin && <Link className="ml-3 text-sky-700" to={`/admin/patients/${p.id}/edit`}>Edit</Link>}</td></tr>)}</tbody></table></div></>; }
